<?php
/**
 * @var \Zend\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\ItemRepresentation[] $data
 * @var array $options
 */

$translate = $this->plugin('translate');
?>
<ul>
<?php foreach ($data['narrowers'] as $children): ?>
    <li><?= $children['current']->linkPretty() ?>
    <?php if (!empty($children['narrowers'])): ?>
        <?= $this->partial('common/ead-tree-recursive', ['data' => $children['narrowers'], 'options' => $options]) ?>
   <?php endif; ?>
   </li>
<?php endforeach; ?>
</ul>
